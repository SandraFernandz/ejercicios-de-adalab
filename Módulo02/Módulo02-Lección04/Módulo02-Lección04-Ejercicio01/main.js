'use strict';

//en cuanto tenemos más de una línea de código necesitamos usar paréntesis para la función arrow. además, la función arrow con paréntesis siempre espera return para efectuarse.

const multFunction = (a, b) =>{
    return a * b
}

console.log(multFunction(6,9));